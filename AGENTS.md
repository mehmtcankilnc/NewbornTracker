# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

This project is pinned to Expo SDK 54 to match the user's installed Expo Go app (client 54.0.8). Do not upgrade the `expo` package past `^54.0.0` without confirming the target Expo Go / dev client version first.
